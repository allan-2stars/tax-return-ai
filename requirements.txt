# Web framework
fastapi>=0.111
uvicorn[standard]>=0.29

# Validation
pydantic>=2.7
pydantic-settings>=2.2

# Database
sqlalchemy>=2.0
alembic>=1.13
aiosqlite>=0.20          # SQLite async driver
asyncpg>=0.29            # PostgreSQL async driver

# File handling
python-multipart>=0.0.9  # upload support
pdfplumber>=0.11         # PDF text extraction
pytesseract>=0.3         # OCR fallback
Pillow>=10.0             # image support for OCR

# AI
anthropic>=0.30
openai>=1.30

# Schema validation
jsonschema>=4.21

# Storage
boto3>=1.34              # S3-compatible storage

# Crypto / hashing (stdlib hashlib used for SHA-256 — no extra dep needed)

# Dev / test
pytest>=8.0
pytest-asyncio>=0.23
httpx>=0.27              # async test client
ruff>=0.4                # lint + format
