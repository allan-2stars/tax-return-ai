# Example API Endpoints

```text
GET    /api/health
POST   /api/tax-sessions
GET    /api/tax-sessions/{session_id}
POST   /api/tax-sessions/{session_id}/documents
GET    /api/documents/{document_id}
POST   /api/documents/{document_id}/extract
POST   /api/documents/{document_id}/classify
GET    /api/tax-sessions/{session_id}/review-queue
POST   /api/review-actions
POST   /api/tax-sessions/{session_id}/exports
GET    /api/export-packages/{export_id}
```
